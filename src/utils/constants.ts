const Constants = {
    EMAIL_REGEX: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    difficultyToPointsMap: {
        "easy": 10,
        "medium": 20,
        "hard": 30,
        "expert" : 40
    },
}

export default Constants;